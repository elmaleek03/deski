name: Build & Release (Windows)

on:
  push:
    tags:
      - "v*"       # run only when pushing tags like v0.1.0
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Publish win-x64
        run: dotnet publish -c Release -r win-x64 --self-contained false -o publish

      - name: Archive executable
        run: |
          cd publish
          $version = "${env:GITHUB_REF_NAME}"   # e.g. v0.1.0
          Compress-Archive -Path * -DestinationPath "Deski-$version.zip"
        shell: pwsh

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}                 # e.g. v0.1.0
          name: "Deski ${{ github.ref_name }}"             # "Deski v0.1.0"
          draft: false
          prerelease: false
          files: publish/Deski-${{ github.ref_name }}.zip  # Deski-v0.1.0.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
